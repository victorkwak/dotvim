Installation:
	In home directort (~/): git clone https://github.com/victorkwak/dotvim.git
	
Rename dotvim:
	mv dotvim .vim

Create symlinks:
	ln -s ~/.vim/vimrc ~/.vimrc

Switch to the `~/.vim` directory, and fetch submodules:

    cd ~/.vim
    git submodule init
    git submodule update
